import { CircularSlider } from 'react-web-circular-slider';

export const App = () => {
	return (
		<div className="App">
			<header className="App-header">
				<CircularSlider />
			</header>
		</div>
	);
};
